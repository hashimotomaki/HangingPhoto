<div class="row">
	<div class="col-md-6 col-md-offset-3">


	  <% if @photo.errors.any? %>
			<div id="alert alert-warning">
				<h3><%= @photo.errors.count %>error prohibited this obj from being saved:</h3>
				<ul>
					<% @photo.errors.full_messages.each do |message| %>
					<li><%= message %></li>
					<% end %>
				</ul>
			</div>
		<% end %>


		<h1>投稿の修正</h1>

		<%= render "photos/form", photo: @photo %>
		<%= link_to "詳細", photo_path(@photo), class: "photo_#{@photo.id}" %>
		<%= link_to "戻る", photos_path, class: "back" %>

	</div>
	<div class="col-md-3"></div>

</div>